# RSVP Nano Tweaks v0.0.9-tweaks.3

This release includes all fixes from `v0.0.9-tweaks.2` and refreshes every
installable locale-pack archive after the rotation-label translations changed.

## CI fix

- Regenerate `locale-packs/*.zip` and `locale-packs/index.json` so the checked-in
  language packs match `localization/strings.toml`.
- Verify locale-pack generation and all locale-pack unit tests.

## Waveshare 3.49 Rev2 OTA

Use `rsvp-nano-esp32-s3-touch-lcd-3.49-rev2-ota.bin` and set the Companion
release tag to `v0.0.9-tweaks.3`.
